# ТЗ: Прототип горизонтального скрола таблицы конкурентов

> Figma-источник: [Отчёт о цене и конкурентах, node 2823:127279](https://www.figma.com/design/whWaY1l1DYcVDJqHJiXJk2/%D0%9E%D1%82%D1%87%D1%91%D1%82-%D0%BE-%D1%86%D0%B5%D0%BD%D0%B5-%D0%B8-%D0%BA%D0%BE%D0%BD%D0%BA%D1%83%D1%80%D0%B5%D0%BD%D1%82%D0%B0%D1%85?node-id=2823-127279)

---

## 1. Цель

Сверстать полную страницу «Обзор конкурентов» для эксперимента с горизонтальным скролом широкой таблицы объявлений. Главный фокус — синхронный горизонтальный скрол двух связанных таблиц.

---

## 2. Общий лейаут страницы

```
┌──────────────────────────────────────────────────────────┐
│                    Header (100% × 67px)                  │
├────────────┬────┬───────────────────────────────────────┤
│            │    │          Report Section                │
│  Sidebar   │ S  │                                        │
│  244px     │ T  │  ┌── Report Header ──────────────────┐ │
│  fixed     │ E  │  │  «Отчёт о цене и конкурентах»     │ │
│            │ P  │  │  1-комн. кв., 50 м², 2/12 этаж    │ │
│  Меню:     │ P  │  │  Адрес                             │ │
│  - Сводка  │ E  │  ├── Report Summary ─────────────────┤ │
│  - Мои     │ R  │  │  ① «Обзор конкурентов» + описание │ │
│    объявл. │    │  │                                     │ │
│  - Уведом. │48px│  │  ┌─ ТАБЛИЦА А (объект оценки) ──┐  │ │
│  - Сообщ.  │    │  │  │  Шапка колонок                │  │ │
│  - Пакеты  │ ①  │  │  │  1 строка: мой объект         │  │ │
│  - Кошелёк │ |  │  │  └──────── overflow-x ───────────┘  │ │
│  - Отчёты  │ |  │  │                                     │ │
│  - ...     │ |  │  ├── Tabs ───────────────────────────┤ │
│            │ |  │  │  «В отчёте N» | «Подборка конк.»  │ │
│            │ |  │  ├── Filters / Results Counter ──────┤ │
│            │ |  │  │                                     │ │
│            │ |  │  │  ┌─ ТАБЛИЦА Б (конкуренты) ──────┐  │ │
│            │ |  │  │  │  до 5/10/... строк конкурентов │  │ │
│            │ |  │  │  └──────── overflow-x ───────────┘  │ │
│            │ |  │  │                                     │ │
│  Настройки │ ②  │  ├── ② Рекомендация по цене ────────┤ │
│  Поддержка │ |  │  │  Чипы + инпуты + шкала «Оценка»  │ │
│  Выйти     │ ③  │  ├── ③ Статистика по объекту ───────┤ │
│            │ |  │  │  Таблица метрик (6 строк)         │ │
│            │ ④  │  ├── ④ Комментарий по объекту ───────┤ │
│            │    │  │  Textarea 600px / 128px           │ │
│            │    │  └──────────────────────────────────┘  │
├────────────┴────┴───────────────────────────────────────┤
│  Sticky Footer (виден если ≥1 объект выбран):            │
│  «Сгенерировать PDF» / «Сохранить и выйти»              │
└──────────────────────────────────────────────────────────┘
```

---

## 3. Ключевая механика: синхронный скрол двух таблиц

### Проблема
На странице две визуально раздельные таблицы с одинаковой структурой колонок:

- **Таблица А** — «Мой объект для оценки» (шапка + 1 строка). Расположена выше табов.
- **Таблица Б** — «Подборка конкурентов» (строки без шапки). Расположена ниже табов и фильтров.

Обе таблицы шире контейнера и имеют горизонтальный скрол.

### Требование
При скроле любой из таблиц вторая должна **синхронно** сдвигаться на ту же позицию. Пользователь в любой момент видит одни и те же колонки в обеих таблицах.

### Реализация

```
Таблица А:  onscroll → scrollLeft → применить к Таблице Б
Таблица Б:  onscroll → scrollLeft → применить к Таблице А
```

- Использовать `scrollLeft` синхронизацию через JS (`addEventListener('scroll', ...)`)
- Предотвратить рекурсивный вызов (флаг `isSyncing`)
- `overflow-x: auto` на обоих контейнерах
- Скроллбар виден только у нижней таблицы (у верхней `overflow-x: auto` но скроллбар скрыт через CSS, либо `overflow-x: hidden` + программный скрол)

### Sticky-колонки
Колонки **Checkbox + Фото** залипают при горизонтальном скроле:
- `position: sticky; left: 0; z-index: 2` для Checkbox
- `position: sticky; left: 40px; z-index: 2` для Фото
- Белый фон на sticky-ячейках, чтобы контент не просвечивал
- Правая тень на последней sticky-колонке для визуального отделения
- **Hover строки** включает sticky-ячейки — CSS `tr:not(.no-hover):hover td.sticky-checkbox, tr:not(.no-hover):hover td.sticky-photo { background: #F3F5FA }`. Класс `.no-hover` на строке-заглушке исключает её из правила

### Единый скроллбар
Один общий скроллбар управляет обеими таблицами:
- Обе таблицы имеют `overflow: hidden` (без собственных скроллбаров)
- Под Таблицей Б размещается **скрытый div-скроллер** с `overflow-x: auto` и внутренним div шириной равной ширине контента таблицы
- При скроле этого div — программно обновляется `scrollLeft` обеих таблиц
- Скроллбар визуально привязан к нижней границе области таблиц

```
┌─────────────────────────────┐
│  ТАБЛИЦА А (overflow:hidden)│  ← scrollLeft управляется программно
│  шапка + 1 строка           │
├─────────────────────────────┤
│  Табы / Фильтры             │  ← не скролятся
├─────────────────────────────┤
│  ТАБЛИЦА Б (overflow:hidden)│  ← scrollLeft управляется программно
│  N строк конкурентов        │
├─────────────────────────────┤
│  ◄══════════■═══════════►   │  ← единственный скроллбар (div-скроллер)
└─────────────────────────────┘
```

---

## 4. Структура колонок таблицы

Обе таблицы разделяют одинаковую сетку колонок:

| # | Колонка | Контент | Мин. ширина | Примечание |
|---|---------|---------|-------------|------------|
| 1 | Checkbox | Чекбокс выбора | 40px | **sticky left: 0** |
| 2 | Фото | Превью изображения | 86px | **sticky left: 40px** |
| 3 | Описание | Комнаты, м², этаж + метро + ЖК + адрес | 260px | Многострочная ячейка |
| 4 | Цена сейчас | Цена ₽ + цена за м² + дельта | 170px | Дельта красная/зелёная |
| 5 | Динамика | Иконка-график изменения цены | 40px | |
| 6 | Срок размещения | Дни + дата публикации | 120px | |
| 7 | Метро | Иконка метро + название станции + автобус + минуты | 120px | |
| 8 | Ремонт | Тип ремонта (напр. «Косметический ремонт») | 140px | |
| 9 | Тип дома | Материал стен (напр. «Кирпичный») | 120px | |
| 10 | Просмотры | Иконка глаза + счётчик (напр. «400 / 2 900») | 120px | Всего / уникальных |

Суммарно: **~1216px** при видимой области **~898px**.

---

## 5. Блоки страницы — детализация

### 5.1 Header
- Высота: 67px, 100% ширины
- Лого ЦИАН слева
- Справа: «Для профи», «Что нового?», иконки (сообщения, избранное, звонки), кнопка «Разместить объявление», аватар
- Фон: белый, нижняя граница `#d0d8e9`

### 5.2 Sidebar
- Ширина: 244px, фиксирована
- **Фон: `#F3F5FA`**
- Внутренний блок меню: 220px с отступом 12px слева
- Пункты меню (сверху вниз):
  - Сводка
  - Мои объявления (12)
  - Мои уведомления (9+)
  - Сообщения (7)
  - Пакеты размещений →
  - Кошелёк →
  - Отчёты →
  - Мои сделки (100)
  - Заявки (1 238)
  - Электронная регистрация
  - Мои ипотечные заявки
  - Зарабатывайте с Циан
  - Профиль →
  - Качество сервиса
  - Жалобы
  - Скрытые объявления
  - Индивидуальные тарифы
  - Профпоиск
  - Вакансии агентств
- Разделитель
- Нижний блок: Настройки, Поддержка, Выйти
- Высота пункта: 36–40px

### 5.3 Report Header
- Отступ: 36px сверху, 24px справа, 24px снизу от края Report Section (без левого отступа — его обеспечивает stepper)
- Текст: «Отчёт о цене и конкурентах» — Body2, secondary color
- Заголовок: «1-комн. кв., 50 м², 2/12 этаж» — крупный, bold
- Подзаголовок: «Москва, САО, р-н Беговой, Ленинградский проспект 33 А» — Body2, secondary

### 5.4 Report Summary + Таблица А
- Заголовок: «Обзор конкурентов» — Heading3 (18px/700)
- Описание: два абзаца Body2, secondary color
- Шапка таблицы: 10 заголовков колонок (Body2, secondary)
  - «Мой объект для оценки» | «Цена сейчас / Разница со стартовой» | «Срок размещения / Дата публикации» | «Метро» | «Ремонт» | «Тип дома» | «Просмотры»
- **Заголовок первой колонки** — реализован через `colspan="3"` (чекбокс + фото + описание = 40+86+260 = 386px), чтобы не переносился на несколько строк
- **При горизонтальном скроле** (`scrollLeft > 0`) заголовок скрывается (`opacity: 0`, transition 0.15s). Контент строк (sticky-чекбокс, фото, описание) при этом продолжает работать штатно
- Строка объекта оценки:
  - ☑ Checkbox (checked)
  - Фото квартиры
  - «1-комн. кв., 50 м², 2/12 этаж» + м. Белорусская + ЖК «Bolshevik» + адрес
  - 19 130 000 ₽ / 365 000 ₽/м² / −670 000 ₽
  - График динамики
  - 56 дней / 17 декабря 2025
  - м. Белорусская / 🚌 15 мин
  - Косметический ремонт
  - Кирпичный
  - 👁 400 / 2 900

### 5.5 Tabs
- Два таба: **«В отчёте N»** | **«Подборка конкурентов»**
- `N` — счётчик выбранных объектов, обновляется динамически при изменении чекбоксов
- **При загрузке активна вкладка «В отчёте»**
- **Вкладка «Подборка конкурентов»**: показывает список всех конкурентов (с пагинацией +5), фильтры и счётчик результатов видны
- **Вкладка «В отчёте»**: показывает только выбранные объекты (checked), фильтры и счётчик скрыты, кнопка «Показать больше» скрыта
- Нижний разделитель по всей ширине
- НЕ скролится горизонтально

### 5.5.1 Empty state вкладки «В отчёте»
Когда `checkedIds.size === 0` и активна вкладка «В отчёте», Таблица Б показывает заглушку вместо строк.

**Состав заглушки:**
- SVG-иллюстрация (стопка карточек-фото)
- Текст: «Выберите конкурентов, которые подходят для отчёта» — Body1, `--text-secondary`
- Кнопка **«Выбрать вручную»** — primary (фон `--control-primary`, белый текст) → переключает на вкладку «Подборка конкурентов»
- Кнопка **«Заполнить автоматически по подборке»** — text-link (`--text-main`) → добавляет первые 3 не-снятых объекта из `ALL_COMPETITORS` в `checkedIds`, строки появляются с анимацией

**Анимация добавления строк:**
- CSS `@keyframes rowFadeIn`: `opacity 0→1`, `translateY(8px→0)`, длительность 350ms, `ease both`
- Каскадный сдвиг: каждая строка получает `animation-delay = index × 60ms` через JS
- Применяется только при авто-заполнении (не при ручном переключении вкладок)

**Поведение стрелок и hover в пустом состоянии:**
- Обе стрелки принудительно скрыты при `checkedIds.size === 0` на вкладке «В отчёте»
- Строка-заглушка имеет CSS-класс `.no-hover`, исключающий hover-подсветку

### 5.6 Filters
- Видны только на вкладке «Подборка конкурентов», скрыты на «В отчёте»
- Чипы-кнопки (wrap, gap 8px):
  - Радиус 3 км ▾
  - 2 комнаты ▾
  - 10–24 млн ₽ ▾
  - Косметический ремонт ▾
  - Активные и архивные ▾
  - Фильтр ▾ (×3)
  - «Сбросить фильтры» (текстовая, синяя)
- Справа: «🗺 На карте» (текстовая, синяя)
- Функциональность НЕ нужна, только визуал

### 5.7 Results Counter
- Виден только на вкладке «Подборка конкурентов»
- Текст формируется динамически:
  - Если `checkedIds.size === 0`: «Нашли **N конкурентов**» — без упоминания отчёта
  - Если `checkedIds.size > 0`: «Нашли **N конкурентов,** X уже в отчёте»
- `N` = `ALL_COMPETITORS.length` (общее количество загруженных конкурентов)
- `X` = `checkedIds.size` (количество отмеченных объектов)
- Число склоняется через `pluralize`: конкурент / конкурента / конкурентов
- Обновляется при каждом изменении чекбокса и при инициализации страницы

### 5.8 Таблица Б (конкуренты)
- **Данные**: 19 объектов, хранятся в JS-массиве `ALL_COMPETITORS`. Все объекты — 1-комнатные квартиры в районе м. Белорусская (Ленинградский пр., 1-я/2-я Брестская, Нижняя Масловка, Новолесная, Грузинский вал/пер., Тверская-Ямская, Фадеева, Чаянова). Метро: Белорусская, Маяковская, Динамо, Новослободская.
- **Пагинация**: по умолчанию показывается 5 строк, по клику «Показать больше объектов» — добавляется ещё 5. Когда все строки показаны — кнопка скрывается
- **Рендеринг**: строки генерируются через JS-функцию `makeRow`, tbody перерисовывается при смене вкладки или пагинации
- **ЖК (опционально)**: если `d[3]` непустой — показывается «ЖК «…», адрес»; если пустой — только адрес (большинство объектов без ЖК-бренда)
- **Hover строки**: при наведении вся строка перекрашивается в `#F3F5FA` (включая sticky-ячейки). Правило применяется через `:not(.no-hover)` — строка-заглушка исключена
- **Заголовок первой колонки** — «Конкурентный объект», `colspan="3"`, скрывается при горизонтальном скроле (аналогично Таблице А)

#### Интерактивность чекбоксов
- Клик по чекбоксу строки переключает состояние (checked/unchecked) через event delegation
- Состояние хранится в JS-структуре `Set checkedIds` (индексы отмеченных строк)
- При отметке: обновляется счётчик в табе «В отчёте N», показывается Sticky Footer
- При снятии отметки: обновляется счётчик, Footer скрывается если `checkedIds.size === 0`
- На вкладке «В отчёте» снятие галочки немедленно убирает строку из таблицы
- **Изначально ни один объект не отмечен** (`checkedIds` пустой при загрузке)

#### Первые 4 строки данных (индексы 0–3)

| # | Описание | Цена сейчас | Дни | Метро | Ремонт | Тип дома | Просмотры |
|---|----------|-------------|-----|-------|--------|----------|-----------|
| 1 | 1-комн., 46 м², 4/9 этаж, Ленинградский пр. 31, ЖК «Bolshevik» | 19 800 000 ₽ / 430 000 ₽/м² / −700 000 ₽ | 38 дней / 9 янв 2026 | м. Белорусская / 🚌 7 мин | Косм. ремонт | Кирпичный | 380 / 2 400 |
| 2 | 1-комн., 42 м², 3/5 этаж, 2-я Брестская, 5 | 17 900 000 ₽ / 426 000 ₽/м² / −300 000 ₽ | 62 дня / 15 дек 2025 | м. Белорусская / 🚌 11 мин | Без ремонта | Кирпичный | 570 / 3 600 |
| 3 | 1-комн., 54 м², 10/14 этаж, Нижняя Масловка 10 | 24 800 000 ₽ / 459 000 ₽/м² / −700 000 ₽ | 28 дней / 19 янв 2026 | м. Белорусская / 🚌 9 мин | Евроремонт | Монолитный | 260 / 1 640 |
| 4 | 1-комн., 44 м², 5/9 этаж, 4-я Тверская-Ямская 8 | 19 000 000 ₽ / 431 000 ₽/м² / −400 000 ₽ | 55 дней / 22 дек 2025 | м. Маяковская / 🚌 8 мин | Косм. ремонт | Панельный | 450 / 2 900 |

- Строка 14 (индекс 13) имеет бейдж «Снято с публикации» и ссылку «✏ Добавить комментарий»
- Остальные 18 строк — активные объявления, ☐ по умолчанию

### 5.9 Action Buttons
- Горизонтальная раскладка (flex row), gap 8px
- «Показать больше объектов» — **secondary** button (фон `#E6F0FF`, текст `#005EDE`), Heading4 (16px/700), padding 10px 16px, border-radius 8px
  - По умолчанию отображается 5 строк; каждый клик добавляет +5
  - Скрывается, когда все 19 конкурентов уже показаны
  - Скрывается на вкладке «В отчёте»
- «Добавить по ссылке» — text button (фон прозрачный, текст `#005EDE`), Heading5 (14px/700), padding 4px 12px
  - **Видна только на вкладке «Подборка конкурентов»**, скрыта на вкладке «В отчёте» (аналогично кнопке «Показать больше»)

### 5.10 Sticky Footer
- Прилипает к низу viewport
- **Скрыт по умолчанию** (`display: none`), появляется когда выбран хотя бы 1 объект (`checkedIds.size > 0`)
- Padding: 12px сверху/снизу, 72px слева, 24px справа
- «Создать PDF-отчёт» — **primary** button (фон `#0468FF`, текст белый), Heading4 (16px/700), padding 10px 16px, border-radius 8px
- «Сохранить и выйти» — **text** button (фон прозрачный, текст `#005EDE`), Heading4 (16px/700), padding 10px 16px
- Верхняя граница `#d0d8e9`

### 5.11 Стили компонентов таблицы

#### Checkbox
- Размер: 16×16px
- Border: 1px solid `--stroke-control-default` (`#D0D8E9`)
- Border-radius: `--radius-s` (2px)
- Фон: `#FFFFFF`
- Checked-состояние: фон `--control-primary` (`#0468FF`), белая галочка

#### Ячейки таблицы (td)
- Padding: 12px (`--unit3`) со всех сторон
- **Исключение** — ячейка чекбокса: padding-top 16px (`--unit4`), padding-bottom 8px (`--unit2`), padding-left/right 12px
- **Исключение** — ячейка динамики (иконка-график): `padding-left: 0; padding-right: 0` — иконка прижата к колонке «Цена сейчас»
- Граница: `border-top: 1px solid --stroke-divider-neutral` (`#F3F5FA`)
- `border-bottom: none`

#### Шапка таблицы (th)
- Padding: 8px (`--unit2`) сверху/снизу, 12px (`--unit3`) слева/справа
- Граница: `border-bottom: 1px solid --stroke-divider-default` (`#D0D8E9`)
- `border-top: none`
- Текст: Body2 (14px/400), `--text-secondary`

#### Цена (основная сумма)
- Стиль: Body1 bold — 16px/24px, font-weight 700
- Цвет: `--text-primary`

#### Цена за м²
- Стиль: Body2 — 14px/20px, font-weight 400
- Цвет: `--text-secondary`

#### Дельта цены
- Стиль: Body2 — 14px/20px, font-weight 400
- Цвет: `--negative` (красный) при отрицательной дельте

#### Срок размещения (дни)
- Стиль: Body1 bold — 16px/24px, font-weight 700
- Цвет: `--text-primary`

#### Дата публикации
- Стиль: Body2 — 14px/20px, font-weight 400
- Цвет: `--text-secondary`

#### Границы строк таблицы
- Строки разделяются через `border-top` (не `border-bottom`)
- Цвет: `--stroke-divider-neutral` (`#F3F5FA`)
- Шапка: `border-bottom` с цветом `--stroke-divider-default` (`#D0D8E9`)

### 5.12 Рекомендация по цене (`price-report-web`)

> Figma-источник: [node 2584:31914](https://www.figma.com/design/whWaY1l1DYcVDJqHJiXJk2/%D0%9E%D1%82%D1%87%D1%91%D1%82-%D0%BE-%D1%86%D0%B5%D0%BD%D0%B5-%D0%B8-%D0%BA%D0%BE%D0%BD%D0%BA%D1%83%D1%80%D0%B5%D0%BD%D1%82%D0%B0%D1%85?node-id=2584-31914)

Блок располагается после Action Buttons (конкуренты). Ширина 898px, gap между подблоками 28px.

#### 5.12.1 Поле рекомендации (`price-input`)

- Heading3: «Рекомендация по цене»
- Body1: «Ваше экспертное мнение о подходящей стоимости для объекта»
- **Переключатель режима** (чипы, gap 8px) — **интерактивен**:
  - «Точная сумма» — chip default (фон белый, граница `#D0D8E9`)
  - «Диапазон» — chip selected (фон `#E6F0FF`, граница `#3686FF`)
  - `cursor: pointer` на чипах
  - По умолчанию активен режим «Диапазон»
- **Поля ввода** переключаются при выборе режима:
  - **Режим «Диапазон»** (flex row, gap 8px): два инпута «от ₽» и «до ₽» — 288px × 44px каждый
  - **Режим «Точная сумма»**: один инпут 288px × 44px, без префикса, суффикс «₽»

#### 5.12.2 Оценка от Циана (`cian-estimate`)

- Body1 primary: «Оценка от Циана»
- Body2 secondary: «Дополним отчёт инфографикой на основе данных из Росреестра о сделках по похожим объектам»
- **Две radio-опции** (вертикально, gap 4px) — **интерактивны**:
  - «Показывать в отчёте» (checked по умолчанию)
  - «Не показывать» — при выборе скрывает блок `price-estimate-web` с анимацией (`max-height` + `opacity`, 0.35s ease)
- **Ценовой диапазон — визуализация** (`price-estimate-web`):
  - Тултип-маркер поверх шкалы:
    - Caption secondary: «Ваше объявление»
    - Body1 white: «19 130 000 ₽»
    - Фон тултипа: `#212C46`, border-radius 4px
    - Стрелка-хвост снизу, точка-маркер под ней
  - Горизонтальная трёхзонная шкала:
    | Зона | Текст | Фон | Цвет текста |
    |------|-------|-----|-------------|
    | Левая | «Ниже рынка» | `#F3F5FA` | `#697797` |
    | Центральная | «Хорошая цена» | `#EBF9E6` | `#227E01` |
    | Правая | «Выше рынка» | `#FFE9EB` | `#C2122D` |
    - Padding: 12px сверху/снизу, 8–36px слева/справа (по зоне)
    - Border-radius: 8px у крайних зон
  - Граничные значения под шкалой (flex row, `justify-content: center`, `gap: 108px`):
    - Левое: «14 900 000 ₽» / «278 000 ₽/м²» — Body2 / Caption secondary
    - Правое: «16 200 000 ₽» / «324 000 ₽/м²» — Body2 / Caption secondary

---

### 5.13 Статистика по объекту (`statistics-report-web`)

> Figma-источник: [node 2584:31915](https://www.figma.com/design/whWaY1l1DYcVDJqHJiXJk2/%D0%9E%D1%82%D1%87%D1%91%D1%82-%D0%BE-%D1%86%D0%B5%D0%BD%D0%B5-%D0%B8-%D0%BA%D0%BE%D0%BD%D0%BA%D1%83%D1%80%D0%B5%D0%BD%D1%82%D0%B0%D1%85?node-id=2584-31915)

Блок идёт следом за `price-report-web`, gap 56px между ними. Ширина 898px.

- Heading3: «Статистика по объекту»
- Body2 secondary: «Так клиент сможет сопоставить ситуацию на рынке с активностью по его объявлению»
- **Две radio-опции** (вертикально, gap 4px) — **интерактивны**:
  - «Показывать в отчёте» (checked по умолчанию)
  - «Не показывать» — при выборе скрывает таблицу метрик с анимацией (`max-height` + `opacity`, 0.35s ease)

#### Таблица метрик

- Ширина: 314px (область значений)
- Шапка (правая часть, flex row, gap 12px):
  - «за 10 дней» — Body2 secondary, 100px
  - «за всё время» — Body2 secondary, 100px
- **6 строк метрик**. В строке:
  - **Чекбокс** (16×16px) — **интерактивен**, `cursor: pointer`
    - Checked: фон `#0468FF`, белая галочка
    - Unchecked: фон белый, граница `--stroke-control-default`
  - Название метрики — Body2 primary
  - Значения «за 10 дней» / «за всё время» — Heading4 bold, 100px, right-align
  - **При снятии галочки**: значения строки меняют цвет на `--text-secondary` и теряют жирность
  - Padding строки: 8px вертикально
  - Разделитель снизу: dashed, `--stroke-divider-default`

| # | Название | За 10 дней | За всё время |
|---|----------|-----------|-------------|
| 1 | Показы | 573 | 28 473 |
| 2 | Уникальные просмотры | 412 | 20 180 |
| 3 | Переходы в контакты | 32 | 1 540 |
| 4 | Добавлено в избранное | 18 | 887 |
| 5 | Звонки | 9 | 454 |
| 6 | Сравнения | 5 | 241 |

---

### 5.14 Комментарий по объекту (`general-comment-report-web`)

> Figma-источник: [node 2584:31916](https://www.figma.com/design/whWaY1l1DYcVDJqHJiXJk2/%D0%9E%D1%82%D1%87%D1%91%D1%82-%D0%BE-%D1%86%D0%B5%D0%BD%D0%B5-%D0%B8-%D0%BA%D0%BE%D0%BD%D0%BA%D1%83%D1%80%D0%B5%D0%BD%D1%82%D0%B0%D1%85?node-id=2584-31916)

Последний блок отчёта, gap 56px после `statistics-report-web`. Ширина 600px.

- Heading3: «Комментарий по объекту»
- **Textarea** (gap 16px от заголовка):
  - Без label
  - Ширина: 600px (растягивается flex)
  - Высота: 128px (фиксированная по макету)
  - Border: 1px solid `--stroke-control-default` (`#D0D8E9`), border-radius 4px
  - Фон: белый
  - Placeholder (Body1, `--text-primary-disabled` `#B1BAD2`):
    «Какая ситуация на рынке, как лучше поступить с ценой, сколько конкурентных предложений рядом и т. д.»
  - Счётчик символов: Caption secondary «0/300», right-align, прижат к нижнему краю
  - Padding: 8px сверху, 4px снизу, 12px горизонтально

### 5.15 Section Stepper Nav (навигация по разделам)

Вертикальная колонка с пронумерованными кружками, расположенная между sidebar и основным контентом. Визуально обозначает разделы отчёта.

#### Лейаут

- Колонка шириной 48px (`padding-left: 24px`)
- Находится внутри обёртки `.report-area` (flex-контейнер: stepper + report-section)
- Отступ 24px слева от sidebar (padding степпера) + 24px справа до контента (padding контентных блоков)

#### Визуал кружков

- Размер: 24×24px, `border-radius: 50%`
- Фон: `#F3F5FA`, без обводки
- Текст: 12px/700, `--text-secondary`
- Содержимое: цифры 1–4

#### Соединительные линии

- Ширина: 1px, цвет `#D0D8E9`
- Отступ 8px от края каждого кружка (сверху и снизу)
- Отдельные сегменты между каждой парой кружков (не сплошная линия)

#### Привязка к разделам

| Кружок | Раздел | `data-stepper-section` |
|--------|--------|----------------------|
| ① | Обзор конкурентов | `.report-summary` |
| ② | Рекомендация по цене | `.price-report-block` |
| ③ | Статистика по объекту | `.stats-block` |
| ④ | Комментарий по объекту | `.comment-block` |

#### Динамическое позиционирование (JS)

Кружки позиционируются абсолютно внутри `.section-stepper`, вертикальная позиция каждого кружка вычисляется по `getBoundingClientRect().top` соответствующего раздела.

**Пересчёт срабатывает при:**
- `resize` окна
- Изменениях DOM внутри `.report-section` (MutationObserver: `childList`, `subtree`, `class`)
- Изменении размеров секций с `data-stepper-section` (ResizeObserver)
- Завершении CSS-анимаций (`transitionend`, `animationend`)

Это обеспечивает корректное смещение кружков при:
- Добавлении строк («Показать больше объектов»)
- Схлопывании/раскрытии блоков («Не показывать» в статистике или оценке от Циана)

**Защита от циклов:** ResizeObserver наблюдает только за конкретными секциями (`[data-stepper-section]`), а не за всем `report-section`, чтобы не вызывать бесконечный пересчёт при изменении `minHeight` заглушки empty state.

---

## 6. Дизайн-токены

### Типографика
| Стиль | Шрифт | Размер | Вес | Line-height | Letter-spacing |
|-------|-------|--------|-----|-------------|---------------|
| Heading3 | Lato | 18px | 700 | 24px | −0.5px |
| Heading4 | Lato | 16px | 700 | 24px | −0.2px |
| Heading5 | Lato | 14px | 700 | 20px | −0.2px |
| Body1 | Lato | 16px | 400 | 24px | 0 |
| Body2 | Lato | 14px | 400 | 20px | 0 |

### Цвета
| Токен | Hex | Применение |
|-------|-----|------------|
| `--background-primary` | `#FFFFFF` | Фон страницы |
| `--text-primary` | `#0D162E` | Основной текст |
| `--text-secondary` | `#697797` | Вторичный текст, описания |
| `--text-main` | `#005EDE` | Ссылки, текстовые кнопки |
| `--control-primary` | `#0468FF` | Primary-кнопки (фон) |
| `--stroke-divider-neutral` | `#F3F5FA` | Разделители строк таблицы |
| `--stroke-divider-default` | `#D0D8E9` | Разделители секций, контролы |
| `--stroke-control-default` | `#D0D8E9` | Границы чипов/чекбоксов |
| `--control-secondary` | `#E6F0FF` | Фон secondary-кнопок |
| `--negative` | красный | Отрицательная дельта цены |

### Отступы и скругления
| Токен | Значение |
|-------|----------|
| `--unit1` | 4px |
| `--unit2` | 8px |
| `--unit3` | 12px |
| `--unit4` | 16px |
| `--radius-s` | 2px |
| `--radius-m` | 4px |
| `--radius-l` | 8px |

---

## 6.1 Стрелки горизонтального скрола (affordance)

### Проблема
Пользователь может не понять, что таблицу можно скроллить горизонтально. Также есть проблема доступности при навигации с клавиатуры.

### Решение
Кнопки-стрелки поверх Таблицы Б, появляющиеся при наведении курсора.

### Визуал
- Круглая кнопка 40×40px, `border-radius: 50%`
- Фон: `rgba(0,0,0,0.6)`, hover: `rgba(0,0,0,0.75)`
- Иконка: белый шеврон 16×16px (SVG)
- Градиентная подложка 100px — fade от `transparent` к `rgba(255,255,255,0.95)`
- Правая стрелка: прижата к правому краю таблицы
- Левая стрелка: прижата к `left: 126px` (за sticky-колонками checkbox + фото)

### Hover-зона
Стрелки видны только при `:hover` на `.table-nav-zone` (обёртка Таблицы Б). Для клавиатуры — `:focus-within`.

### Логика видимости
| Состояние | `scrollLeft` | Стрелка ← | Стрелка → |
|-----------|-------------|-----------|-----------|
| Старт | 0–259px | скрыта | видна |
| Описание ушло | ≥ 260px | видна | видна |
| Конец | max | видна | скрыта |
| **Empty state** («В отчёте», нет объектов) | — | **скрыта** | **скрыта** |

**Порог появления левой стрелки: `scrollLeft >= 260px`** — соответствует моменту, когда колонка «Описание» (260px) полностью скрывается за sticky-областью. До этого порога пользователь ещё видит описание и стрелка назад не нужна.

### Скрол по клику
- Шаг = `tableB.clientWidth - 126px` (видимая область минус sticky-колонки)
- Если оставшийся контент < шага — доскроллить до конца
- Анимация: easeInOutCubic, 400ms, через `requestAnimationFrame`
- Скрол применяется к scroll proxy → синхронно двигает обе таблицы

### Viewport-aware позиционирование
Если таблица выше viewport (много строк), стрелка центрируется не по всей таблице, а по **видимой части**:
- `visibleTop = max(tableTop, 67px + stickyHeaderHeight)`
- `visibleBottom = min(tableBottom, viewport - 68px)`
- Стрелка: `top = center(visibleTop, visibleBottom)` в процентах от высоты таблицы
- Пересчитывается на `scroll` (passive) и `resize`

### Доступность
- `<button>` с `aria-label` («Прокрутить влево» / «Прокрутить вправо»)
- `tabindex="0"`, `type="button"`
- `focus-visible` outline: `2px solid --control-primary`

---

## 6.2 Sticky шапка таблицы конкурентов

### Проблема
При большом списке конкурентов (19 строк) пользователь теряет контекст колонок при вертикальном скроле.

### Решение
Фиксированная шапка появляется, когда фильтры уходят из viewport.

### Визуал
- `position: fixed; top: 67px; left: 244px; right: 0`
- Фон: `--background-primary`, `border-bottom: 1px solid --stroke-divider-default`
- `padding: 0 24px` — выравнивание с таблицами
- Появление: `opacity 0→1` + `transform: translateY(-100%) → 0`, transition 0.2s

### Отличие от шапки Таблицы А
- Первая колонка: **«Конкурентный объект»** (не «Мой объект для оценки»)
- Остальные колонки идентичны

### Логика появления
`IntersectionObserver` на `.filters` с `rootMargin: '-67px 0px 0px 0px'`:
- Фильтры видны → шапка скрыта
- Фильтры ушли за верхний край (с учётом 67px header) → шапка появляется
- На вкладке «В отчёте» шапка принудительно скрыта (фильтры не видны, но IntersectionObserver не должен срабатывать)

### Горизонтальная синхронизация
- `.sticky-header-inner` имеет `overflow: hidden`
- `scrollLeft` синхронизируется с scroll proxy при каждом scroll-событии
- Sticky-колонки (checkbox + фото) залипают внутри шапки

---

## 6.3 Точка входа: страница «Мои объявления» (`mo.html`)

> Figma-источник: [node 2704:14995](https://www.figma.com/design/whWaY1l1DYcVDJqHJiXJk2/%D0%9E%D1%82%D1%87%D1%91%D1%82-%D0%BE-%D1%86%D0%B5%D0%BD%D0%B5-%D0%B8-%D0%BA%D0%BE%D0%BD%D0%BA%D1%83%D1%80%D0%B5%D0%BD%D1%82%D0%B0%D1%85?node-id=2704-14995)

Отдельная страница-прототип, демонстрирующая точку входа в отчёт по цене. Располагается в `mo.html` рядом с `index.html`.

### Лейаут

Общий лейаут идентичен `index.html`: Header (67px) + Sidebar (244px) + Content area. Активный пункт sidebar — «Мои объявления».

### Контентная область (сверху вниз)

1. **Заголовок** «Мои объявления» + поле поиска (placeholder, без функциональности)
2. **Табы статусов**: Активные 12 | Неактивные 23 | Отклонённые 2 | Черновик 17 | Архив 4
3. **Фильтры категорий**: чипы (Все/Аренда/Продажа | Все/Квартиры и комнаты/Загородка/Коммерческая)
4. **Дропдауны**: Всё агентство, Тип размещения, Статус проверки, За счёт агентства
5. **Чекбокс-фильтры**: Размещённые вручную, Видимые только агентам, Без фото, Неактуальные, Только с дублями, Со скидкой на ипотеку
6. **Карточки стратегий**: «Автостратегия» и «Гибкая стратегия» (только визуал)
7. **Сортировка**: «4 объявления отсортированы по дате размещения» + кнопки «Обновить даты», «Настроить календарь»
8. **Чекбокс** «Выбрать все на странице»
9. **Карточки объявлений** (4 штуки)

### Карточка объявления (горизонтальная строка)

| Блок | Содержимое | Ширина |
|------|-----------|--------|
| Чекбокс | Без функциональности | 32px |
| Фото | Превью из `photos/` | 80×80px |
| Описание | Комнаты, м², этаж / Метро / ЖК, адрес | 210px |
| **Цена** | Цена ₽ + цена за м² + **кнопка «Отчёт о цене»** | 140px |
| Статистика | Просмотры, звонки, сообщения, избранное (+дельты) | 110px |
| Позиция | N позиция по району / Ставка N ₽ / бейджи Топ | 140px |
| Дата | Время + «осталось N д.» | 100px |
| Действия | «...» / ✏ / ▦ / «Поднять» / «Отправить рассылку» | flex |

### Кнопка «Отчёт о цене»

- Стиль: outlined — синяя рамка `#0468FF`, синий текст, белый фон
- Размер: compact (13px/700, padding 4px 10px, border-radius 4px)
- Иконка: `MO/Group 5281.svg` (график со стрелкой)
- Расположение: под ценой за м² в каждой карточке
- По клику: переход на `index.html`

### Онбординг-тултип

Анонс новой фичи — показывается над кнопкой «Отчёт о цене» **первого** объявления при загрузке.

- Фон: `#2B3147`, border-radius 8px
- Бейдж **«Новое»** — `#E84260`, в правом верхнем углу тултипа
- Текст: «Отчёт о цене и конкурентах» — body2, белый
- Стрелка-хвост снизу (CSS border-triangle), указывает на кнопку
- Кнопка закрытия (×) — полупрозрачный белый
- Закрывается по клику на × или по клику на «Отчёт о цене»
- Анимация появления: `opacity 0→1` + `translateY(6px→0)`, 0.3s ease

### Данные объявлений

| # | Описание | Цена | За м² | Метро |
|---|----------|------|-------|-------|
| 1 | 3-комн. кв., 50 м², 2/12 этаж | 12 900 000 ₽ | 258 000 ₽/м² | Белорусская |
| 2 | 2-комн. кв., 46 м², 12/16 этаж | 19 300 000 ₽ | 420 000 ₽/м² | Котельники |
| 3 | 1-комн. кв., 38 м², 9/9 этаж | 9 100 000 ₽ | 240 000 ₽/м² | Деловой Центр |
| 4 | 1-комн. апарт., 85 м², 24/85 этаж | 15 850 000 ₽ | 186 000 ₽/м² | Площадь Ильича |

**Консистентность данных**: первое объявление на `mo.html` соответствует объекту оценки на `index.html` (совпадают описание, адрес, ЖК, метро).

### Навигация

- `mo.html` → `index.html`: через кнопку «Отчёт о цене»
- `index.html` → `mo.html`: через пункт sidebar «Мои объявления»

---

## 7. Технические ограничения и допущения

- **Данные**: захардкожены в JS-массиве `ALL_COMPETITORS` (19 объектов), без API
- **Фильтры**: только визуал, без функциональности
- **Sidebar**: полностью верстается, ссылка «Мои объявления» ведёт на `mo.html`
- **Адаптивность**: только десктоп (1269px+)
- **Браузеры**: современные (Chrome, Firefox, Safari)
- **Изображения**: заглушки или placeholder-картинки

---

## 8. Стек

**HTML + CSS + vanilla JS**, без сборки. Открывается в браузере напрямую или через локальный сервер.

- Шрифт Lato — Google Fonts CDN
- CSS-ресет включает `input, button, textarea, select { font-family: inherit; font-size: inherit; }`

### Общие компоненты

Header, sidebar, CSS-токены и базовые стили вынесены в shared-файлы, чтобы изменения применялись на обеих страницах одновременно:

- **`shared.css`** — CSS-переменные, reset, типографика, стили header/sidebar/layout, metro-иконки
- **`header.js`** — `renderHeader()` вставляет HTML хедера вместо `<div id="header">`
- **`sidebar.js`** — `renderSidebar({ active: '...' })` вставляет HTML сайдбара с подсветкой активного пункта

Подключение на странице:
```html
<link rel="stylesheet" href="shared.css">
<!-- в <body>: -->
<div id="header"></div>
<div class="layout">
  <div id="sidebar"></div>
  <main>...</main>
</div>
<script src="header.js"></script>
<script src="sidebar.js"></script>
<script>
  renderHeader();
  renderSidebar({ active: 'Мои объявления' });
</script>
```

Каждая страница хранит только свои уникальные стили в `<style>`.

### Файловая структура

```
prototype/
├── horizontal-scroll-prototype-spec.md  ← эта спека
└── src/
    ├── shared.css   ← общие стили (токены, header, sidebar, layout)
    ├── header.js    ← компонент хедера
    ├── sidebar.js   ← компонент сайдбара
    ├── index.html   ← страница отчёта (исходная версия)
    ├── report.html  ← страница отчёта (с section stepper nav)
    ├── mo.html      ← страница «Мои объявления» (точка входа)
    ├── photos/      ← фото квартир (1.png–12.png)
    └── MO/          ← ассеты (logo.svg, Group 5281.svg)
```

### Деплой (GitHub Pages)

Прототип публикуется через репозиторий `rsadovnikov/rsadovnikov.github.io` (GitHub Pages).

- **Продакшн-URL**: https://rsadovnikov.github.io/report-prototype/
- **Локальный путь к репо**: `/Users/r.sadovnikov/Documents/portfolio/`
- **Целевая папка**: `report-prototype/` (в корне репо)
- **Процесс деплоя**: скопировать файлы из `prototype/src/` → `portfolio/report-prototype/`, закоммитить и запушить в `main`
